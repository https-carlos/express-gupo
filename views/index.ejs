<!DOCTYPE html>
<html lang="pt-br">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <link href="./styles.css" rel="stylesheet" type="text/css">
    <title>Usuários</title>
</head>

<body>
    <div class="container">
        <div class="row">
            <div class="col-sm-8">
                <h1>Usuários</h1>
            </div>
            <div class="col-sm-4 text-right">
                <a href="registro" class="btn btn-outline-primary" role="button" onclick="AdicionarUser()">Adicionar
                    novo usuário</a>
            </div>
        </div>
        <table class="table table-striped">
            <div class="table-head">
                <thead>
                    <tr>
                        <th>ID</th>
                        <th>Nome</th>
                        <th>Data de Nascimento</th>
                        <th>Sexo</th>
                        <th>Ações</th>
                    </tr>
                </thead>
            </div>
            <tbody>
                <% for(let i=0; i < users.length; i++) { %>
                    <tr>
                        <td>
                            <%= users[i].id %>
                        </td>
                        <td>
                            <%= users[i].nome %>
                        </td>
                        <td>
                            <%= users[i].data_nascimento %>
                        </td>
                        <td>
                            <%= users[i].sexo %>
                        </td>
                        <td>
                            <button type="button" class="btn btn-primary" onclick="AtualizarUser()">Atualizar</button>
                            <form method="post" action="/excluir/<%= users[i].id %>" class="d-inline">
                                <button type="submit" class="btn btn-danger">Excluir</button>
                            </form>
                        </td>
                    </tr>
                    <% } %>
            </tbody>
        </table>
    </div>
</body>

</html>
