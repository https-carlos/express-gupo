<!DOCTYPE html>
<html lang="pt-br">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <link href="./stylesR.css" rel="stylesheet" type="text/css">
    <title>Cadastro</title>
</head>

<body>
    <div class="container">
        <h2>Dados pessoais</h2>
        <hr>
        <form action="validacao" method="post">
            <!-- Se houver erro no nome -->
            <% if (error.nome) { %>
                <p class="title">Nome</p>
                <input type="text" class="form-control is-invalid" id="nome" name="nome" placeholder="Meu nome"
                    value="<%= dados ? dados.nome : '' %>">
                <div class="invalid-feedback">
                    <%= error.nome %>
                </div>
                <!-- Se o nome for válido -->
                <% } else { %>
                    <div class="mb-3">
                        <label for="nome" class="form-label">
                            <p class="title">Nome</p>
                        </label>
                        <input type="text" class="form-control" id="nome" name="nome" placeholder="Meu nome"
                            value="<%= dados ? dados.nome : '' %>">
                    </div>
                    <% } %>

                        <div class="row">
                            <div class="col">
                                <label for="cpf">
                                    <p class="title">CPF</p>
                                </label>
                                <!-- Se houver erro no CPF -->
                                <% if (error.cpf) { %>
                                    <input class="form-control is-invalid" type="text" id="cpf" name="cpf"
                                        placeholder="000.000.000-00" aria-label="default input example"
                                        value="<%= dados ? dados.cpf : '' %>">
                                    <div class="invalid-feedback">
                                        <%= error.cpf %>
                                    </div>
                                    <!-- Se o CPF for válido -->
                                    <% } else { %>
                                        <input class="form-control" type="text" id="cpf" name="cpf"
                                            placeholder="000.000.000-00" aria-label="default input example"
                                            value="<%= dados ? dados.cpf : '' %>">
                                        <% } %>
                            </div>
                            <div class="col">
                                <label for="data_nascimento">
                                    <p class="title">Data de Nascimento</p>
                                </label>
                                <!-- Se houver erro na data de nascimento -->
                                <% if (error.data_nascimento) { %>
                                    <input class="form-control is-invalid" type="date" id="data_nascimento"
                                        name="data_nascimento" aria-label="default input example"
                                        value="<%= dados ? dados.data_nascimento : '' %>">
                                    <div class="invalid-feedback">
                                        <%= error.data_nascimento %>
                                    </div>
                                    <!-- Se a data de nascimento for válida -->
                                    <% } else { %>
                                        <input class="form-control" type="date" id="data_nascimento"
                                            name="data_nascimento" aria-label="default input example"
                                            value="<%= dados ? dados.data_nascimento : '' %>">
                                        <% } %>
                            </div>
                            <div class="col">
                                <p class="title">Sexo</p>
                                <div class="form-check">
                                    <input class="form-check-input" type="radio" name="sexo" id="masculino"
                                        value="masculino" checked>
                                    <label class="form-check-label" for="masculino">
                                        Masculino
                                    </label>
                                </div>
                                <div class="form-check">
                                    <input class="form-check-input" type="radio" name="sexo" id="feminino"
                                        value="feminino">
                                    <label class="form-check-label" for="feminino">
                                        Feminino
                                    </label>
                                </div>
                            </div>
                        </div>

                        <div class="row">
                            <div class="col">
                                <p class="title">Estado Civil</p>
                                <select class="form-select" id="estado_civil" name="estado_civil"
                                    aria-label="Estado Civil">
                                    <option value="solteiro">Solteiro(a)</option>
                                    <option value="casado">Casado(a)</option>
                                    <option value="divorciado">Divorciado(a)</option>
                                    <option value="viuvo">Viúvo(a)</option>
                                </select>
                            </div>
                            <div class="col">
                                <label for="renda_mensal">
                                    <p class="title">Renda mensal</p>
                                </label>
                                <!-- Se houver erro na renda mensal -->
                                <% if (error.renda_mensal) { %>
                                    <input class="form-control is-invalid" type="text" id="renda_mensal"
                                        name="renda_mensal" placeholder="0.00" aria-label="default input example"
                                        value="<%= dados ? dados.renda_mensal : '' %>">
                                    <div class="invalid-feedback">
                                        <%= error.renda_mensal %>
                                    </div>
                                    <!-- Se a renda mensal for válida -->
                                    <% } else { %>
                                        <input class="form-control" type="text" id="renda_mensal" name="renda_mensal"
                                            placeholder="0.00" aria-label="default input example"
                                            value="<%= dados ? dados.renda_mensal : '' %>">
                                        <% } %>
                            </div>
                        </div>

                        <h2 class="mt-5">Endereço</h2>
                        <hr>

                        <div class="row">
                            <div class="col">
                                <label for="logradouro">
                                    <p class="title">Logradouro</p>
                                </label>
                                <!-- Se houver erro no logradouro -->
                                <% if (error.logradouro) { %>
                                    <input class="form-control is-invalid" type="text" id="logradouro" name="logradouro"
                                        placeholder="Av. Paulista" aria-label="default input example"
                                        value="<%= dados ? dados.logradouro : '' %>">
                                    <div class="invalid-feedback">
                                        <%= error.logradouro %>
                                    </div>
                                    <!-- Se o logradouro for válido -->
                                    <% } else { %>
                                        <input class="form-control" type="text" id="logradouro" name="logradouro"
                                            placeholder="Av. Paulista" aria-label="default input example"
                                            value="<%= dados ? dados.logradouro : '' %>">
                                        <% } %>
                            </div>
                            <div class="col">
                                <label for="numero">
                                    <p class="title">Número</p>
                                </label>
                                <!-- Se houver erro no número -->
                                <% if (error.numero) { %>
                                    <input class="form-control is-invalid" type="text" id="numero" name="numero"
                                        placeholder="Nº" aria-label="default input example"
                                        value="<%= dados ? dados.numero : '' %>">
                                    <div class="invalid-feedback">
                                        <%= error.numero %>
                                    </div>
                                    <!-- Se o número for válido -->
                                    <% } else { %>
                                        <input class="form-control" type="text" id="numero" name="numero"
                                            placeholder="Nº" aria-label="default input example"
                                            value="<%= dados ? dados.numero : '' %>">
                                        <% } %>
                            </div>
                            <div class="col">
                                <label for="complemento">
                                    <p class="title">Complemento</p>
                                </label>
                                <input class="form-control" type="text" id="complemento" name="complemento"
                                    placeholder="Casa, Apto, Loja" aria-label="default input example">
                            </div>
                        </div>

                        <div class="row">
                            <div class="col-4">
                                <label for="estado">
                                    <p class="title">Estado</p>
                                </label>
                                <select class="form-select" id="estado" name="estado" aria-label="Estado">
                                    <option value="AC">Acre</option>
                                    <option value="AL">Alagoas</option>
                                    <option value="AP">Amapá</option>
                                    <option value="AM">Amazonas</option>
                                    <option value="BA">Bahia</option>
                                    <option value="CE">Ceará</option>
                                    <option value="DF">Distrito Federal</option>
                                    <option value="ES">Espírito Santo</option>
                                    <option value="GO">Goiás</option>
                                    <option value="MA">Maranhão</option>
                                    <option value="MT">Mato Grosso</option>
                                    <option value="MS">Mato Grosso do Sul</option>
                                    <option value="MG">Minas Gerais</option>
                                    <option value="PA">Pará</option>
                                    <option value="PB">Paraíba</option>
                                    <option value="PR">Paraná</option>
                                    <option value="PE">Pernambuco</option>
                                    <option value="PI">Piauí</option>
                                    <option value="RJ">Rio de Janeiro</option>
                                    <option value="RN">Rio Grande do Norte</option>
                                    <option value="RS">Rio Grande do Sul</option>
                                    <option value="RO">Rondônia</option>
                                    <option value="RR">Roraima</option>
                                    <option value="SC">Santa Catarina</option>
                                    <option value="SP">São Paulo</option>
                                    <option value="SE">Sergipe</option>
                                    <option value="TO">Tocantins</option>
                                </select>
                            </div>
                            <div class="col-8">
                                <label for="cidade">
                                    <p class="title">Cidade</p>
                                </label>
                                <!-- Se houver erro na cidade -->
                                <% if (error.cidade) { %>
                                    <input class="form-control is-invalid" type="text" id="cidade" name="cidade"
                                        placeholder="Sua cidade" aria-label="default input example"
                                        value="<%= dados ? dados.cidade : '' %>">
                                    <div class="invalid-feedback">
                                        <%= error.cidade %>
                                    </div>
                                    <!-- Se a cidade for válida -->
                                    <% } else { %>
                                        <input class="form-control" type="text" id="cidade" name="cidade"
                                            placeholder="Sua cidade" aria-label="default input example"
                                            value="<%= dados ? dados.cidade : '' %>">
                                        <% } %>
                            </div>
                        </div>

                        <button type="submit" class="btn btn-primary mt-4">Cadastrar</button>
        </form>
    </div>
</body>

</html>